name: Apply unified persistence UI

on:
  pull_request:
    branches: [main]
    paths:
      - scripts/apply-unified-persistence-ui.py
      - .github/workflows/apply-unified-persistence-ui.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'feature/unified-persistence-ui'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: feature/unified-persistence-ui
          fetch-depth: 0
      - name: Apply and validate unified persistence UI
        run: |
          python3 scripts/apply-unified-persistence-ui.py
          PYTHONPATH=gui python3 -m unittest discover -s gui -p 'test_*.py'
          python3 -m py_compile gui/*.py
          bash -n scripts/*.sh
          git diff --check
          rm scripts/apply-unified-persistence-ui.py .github/workflows/apply-unified-persistence-ui.yml
          test ! -e scripts/apply-unified-persistence-ui.py
          test ! -e .github/workflows/apply-unified-persistence-ui.yml
      - name: Commit validated product diff
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'gui: unify persistence with the main writer'
          git push origin HEAD:feature/unified-persistence-ui
