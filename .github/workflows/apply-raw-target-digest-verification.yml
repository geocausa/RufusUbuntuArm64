name: Apply raw target-digest verification

on:
  pull_request:
    types: [opened, reopened, ready_for_review]
    branches: [stage2/0.13.0]

permissions:
  contents: write

concurrency:
  group: apply-raw-target-digest-verification
  cancel-in-progress: false

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'audit/raw-target-digest-verification'
    runs-on: ubuntu-24.04
    steps:
      - name: Check out the integration branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: audit/raw-target-digest-verification
          fetch-depth: 0

      - name: Set up declared Go version
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff
        with:
          go-version-file: go.mod
          cache: false

      - name: Apply the reviewed patch
        run: python3 scripts/apply-raw-target-digest-verification.py

      - name: Format and run focused regressions
        run: |
          gofmt -w internal/imaging/imaging.go internal/imaging/imaging_test.go cmd/rufus-linux/main.go internal/operationcost/contract.go internal/operationcost/contract_test.go
          go test ./internal/imaging ./internal/operationcost ./cmd/rufus-linux
          python3 -m json.tool docs/operation-cost-contract.json >/dev/null
          git diff --check

      - name: Remove the one-shot executor and publish the reviewed source commit
        run: |
          rm scripts/apply-raw-target-digest-verification.py
          rm .github/workflows/apply-raw-target-digest-verification.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'Audit: verify raw writes against authenticated target digests'
          git push origin HEAD:audit/raw-target-digest-verification
