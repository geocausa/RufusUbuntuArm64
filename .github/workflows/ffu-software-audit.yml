name: FFU trusted-utility patch validation

on:
  pull_request:
    branches:
      - stage3/0.14.0
    paths:
      - '.github/workflows/ffu-software-audit.yml'

permissions:
  contents: write

jobs:
  patch-and-validate:
    if: ${{ github.head_ref == 'feature/ffu-trusted-utilities' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Check out exact feature branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: feature/ffu-trusted-utilities
          fetch-depth: 0

      - name: Apply exact trusted-utility changes
        shell: bash
        run: |
          set -euo pipefail
          test ! -e internal/trustedexec/trustedexec_linux.go || exit 0
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new, label):
              source = path.read_text(encoding="utf-8")
              if source.count(old) != 1:
                  raise SystemExit(f"{label}: expected one source anchor, found {source.count(old)}")
              path.write_text(source.replace(old, new), encoding="utf-8")

          trusted = Path("internal/trustedexec")
          trusted.mkdir(parents=True, exist_ok=True)
          (trusted / "trustedexec_linux.go").write_text(r'''//go:build linux

package trustedexec

import (
	"errors"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"syscall"
)

var allowedUtilities = map[string]struct{}{
	"blockdev": {},
	"findmnt":  {},
	"lsblk":    {},
	"umount":   {},
	"wipefs":   {},
}

var systemUtilityRoots = []string{"/usr/bin", "/usr/sbin", "/bin", "/sbin"}

// Resolve returns one root-owned executable from an allowlisted system
// directory. Ambient PATH is never consulted.
func Resolve(name string) (string, error) {
	return resolveAt(name, systemUtilityRoots, 0)
}

func resolveAt(name string, roots []string, trustedUID uint32) (string, error) {
	if strings.TrimSpace(name) != name || name == "" || filepath.Base(name) != name {
		return "", fmt.Errorf("invalid trusted utility name %q", name)
	}
	if _, ok := allowedUtilities[name]; !ok {
		return "", fmt.Errorf("utility %q is not allowlisted", name)
	}
	if len(roots) == 0 {
		return "", errors.New("trusted utility roots are empty")
	}

	canonicalRoots := make([]string, 0, len(roots))
	for _, root := range roots {
		clean := filepath.Clean(root)
		if !filepath.IsAbs(clean) || clean == string(filepath.Separator) {
			return "", fmt.Errorf("invalid trusted utility root %q", root)
		}
		resolved, err := filepath.EvalSymlinks(clean)
		if err != nil {
			if os.IsNotExist(err) {
				continue
			}
			return "", fmt.Errorf("resolve trusted utility root %s: %w", clean, err)
		}
		info, err := os.Stat(resolved)
		if err != nil {
			return "", fmt.Errorf("stat trusted utility root %s: %w", resolved, err)
		}
		if err := validateTrustedPath(resolved, info, trustedUID, true); err != nil {
			return "", err
		}
		if !containsPath(canonicalRoots, resolved) {
			canonicalRoots = append(canonicalRoots, resolved)
		}
	}
	if len(canonicalRoots) == 0 {
		return "", errors.New("no trusted utility root is available")
	}

	var rejected []string
	for _, root := range canonicalRoots {
		candidate := filepath.Join(root, name)
		resolved, err := filepath.EvalSymlinks(candidate)
		if err != nil {
			if os.IsNotExist(err) {
				continue
			}
			rejected = append(rejected, fmt.Sprintf("%s: %v", candidate, err))
			continue
		}
		if !beneathAnyRoot(resolved, canonicalRoots) {
			rejected = append(rejected, fmt.Sprintf("%s resolves outside trusted roots", candidate))
			continue
		}
		info, err := os.Stat(resolved)
		if err != nil {
			rejected = append(rejected, fmt.Sprintf("%s: %v", resolved, err))
			continue
		}
		if err := validateTrustedPath(resolved, info, trustedUID, false); err != nil {
			rejected = append(rejected, err.Error())
			continue
		}
		return resolved, nil
	}
	if len(rejected) != 0 {
		return "", fmt.Errorf("no safe %s utility found: %s", name, strings.Join(rejected, "; "))
	}
	return "", fmt.Errorf("trusted utility %s was not found", name)
}

func validateTrustedPath(path string, info os.FileInfo, trustedUID uint32, directory bool) error {
	stat, ok := info.Sys().(*syscall.Stat_t)
	if !ok {
		return fmt.Errorf("trusted path %s has no Linux ownership metadata", path)
	}
	if uint32(stat.Uid) != trustedUID {
		return fmt.Errorf("trusted path %s is owned by uid %d, expected %d", path, stat.Uid, trustedUID)
	}
	if info.Mode().Perm()&0o022 != 0 {
		return fmt.Errorf("trusted path %s is group/world writable", path)
	}
	if directory {
		if !info.IsDir() {
			return fmt.Errorf("trusted utility root %s is not a directory", path)
		}
		return nil
	}
	if !info.Mode().IsRegular() {
		return fmt.Errorf("trusted utility %s is not a regular file", path)
	}
	if info.Mode().Perm()&0o111 == 0 {
		return fmt.Errorf("trusted utility %s is not executable", path)
	}
	return nil
}

func beneathAnyRoot(path string, roots []string) bool {
	clean := filepath.Clean(path)
	for _, root := range roots {
		if clean == root || strings.HasPrefix(clean, root+string(filepath.Separator)) {
			return true
		}
	}
	return false
}

func containsPath(paths []string, target string) bool {
	for _, path := range paths {
		if path == target {
			return true
		}
	}
	return false
}
''', encoding="utf-8")

          (trusted / "trustedexec_unsupported.go").write_text(r'''//go:build !linux

package trustedexec

import "fmt"

func Resolve(name string) (string, error) {
	return "", fmt.Errorf("trusted utility %q is supported only on Linux", name)
}
''', encoding="utf-8")

          (trusted / "trustedexec_linux_test.go").write_text(r'''//go:build linux

package trustedexec

import (
	"os"
	"path/filepath"
	"strings"
	"testing"
)

func TestResolveAtAcceptsTrustedExecutable(t *testing.T) {
	root := t.TempDir()
	path := filepath.Join(root, "lsblk")
	if err := os.WriteFile(path, []byte("#!/bin/sh\n"), 0o755); err != nil {
		t.Fatal(err)
	}
	got, err := resolveAt("lsblk", []string{root}, uint32(os.Getuid()))
	if err != nil {
		t.Fatal(err)
	}
	if got != path {
		t.Fatalf("got %s want %s", got, path)
	}
}

func TestResolveAtRejectsUnsafeInputs(t *testing.T) {
	root := t.TempDir()
	path := filepath.Join(root, "lsblk")
	if err := os.WriteFile(path, []byte("x"), 0o644); err != nil {
		t.Fatal(err)
	}
	if _, err := resolveAt("lsblk", []string{root}, uint32(os.Getuid())); err == nil || !strings.Contains(err.Error(), "not executable") {
		t.Fatalf("non-executable utility error = %v", err)
	}
	if _, err := resolveAt("sh", []string{root}, uint32(os.Getuid())); err == nil || !strings.Contains(err.Error(), "not allowlisted") {
		t.Fatalf("non-allowlisted utility error = %v", err)
	}
	if _, err := resolveAt("../lsblk", []string{root}, uint32(os.Getuid())); err == nil {
		t.Fatal("path traversal was accepted")
	}
}

func TestResolveAtRejectsWritableExecutable(t *testing.T) {
	root := t.TempDir()
	path := filepath.Join(root, "lsblk")
	if err := os.WriteFile(path, []byte("x"), 0o755); err != nil {
		t.Fatal(err)
	}
	if err := os.Chmod(path, 0o777); err != nil {
		t.Fatal(err)
	}
	if _, err := resolveAt("lsblk", []string{root}, uint32(os.Getuid())); err == nil || !strings.Contains(err.Error(), "group/world writable") {
		t.Fatalf("writable utility error = %v", err)
	}
}

func TestResolveAtRejectsUnexpectedOwner(t *testing.T) {
	root := t.TempDir()
	path := filepath.Join(root, "lsblk")
	if err := os.WriteFile(path, []byte("x"), 0o755); err != nil {
		t.Fatal(err)
	}
	if _, err := resolveAt("lsblk", []string{root}, uint32(os.Getuid()+1)); err == nil || !strings.Contains(err.Error(), "owned by uid") {
		t.Fatalf("unexpected-owner error = %v", err)
	}
}

func TestResolveAtRejectsSymlinkEscape(t *testing.T) {
	root := t.TempDir()
	outside := t.TempDir()
	target := filepath.Join(outside, "lsblk")
	if err := os.WriteFile(target, []byte("x"), 0o755); err != nil {
		t.Fatal(err)
	}
	if err := os.Symlink(target, filepath.Join(root, "lsblk")); err != nil {
		t.Fatal(err)
	}
	if _, err := resolveAt("lsblk", []string{root}, uint32(os.Getuid())); err == nil || !strings.Contains(err.Error(), "outside trusted roots") {
		t.Fatalf("symlink-escape error = %v", err)
	}
}
''', encoding="utf-8")

          Path("internal/device/utility_linux.go").write_text(r'''//go:build linux

package device

import "github.com/geocausa/RufusArm64/internal/trustedexec"

var resolveDeviceUtility = trustedexec.Resolve
''', encoding="utf-8")

          Path("internal/safety/utility_linux.go").write_text(r'''//go:build linux

package safety

import (
	"context"
	"fmt"
	"os/exec"

	"github.com/geocausa/RufusArm64/internal/device"
	"github.com/geocausa/RufusArm64/internal/trustedexec"
)

var (
	resolveSafetyUtility = trustedexec.Resolve
	findSafetyDevice     = device.Find
)

func trustedSafetyCommandContext(ctx context.Context, name string, args ...string) (*exec.Cmd, error) {
	path, err := resolveSafetyUtility(name)
	if err != nil {
		return nil, fmt.Errorf("resolve trusted utility %s: %w", name, err)
	}
	return exec.CommandContext(ctx, path, args...), nil
}
''', encoding="utf-8")

          device_source = Path("internal/device/device.go")
          replace_once(
              device_source,
              '''\tcmd := exec.CommandContext(ctx,
          \t\t"lsblk", "--json", "--bytes", "--output",
          ''',
              '''\tlsblkPath, err := resolveDeviceUtility("lsblk")
          \tif err != nil {
          \t\treturn nil, fmt.Errorf("resolve trusted lsblk: %w", err)
          \t}
          \tcmd := exec.CommandContext(ctx,
          \t\tlsblkPath, "--json", "--bytes", "--output",
          ''',
              "device lsblk command",
          )

          device_tests = Path("internal/device/device_test.go")
          replace_once(
              device_tests,
              '''import (
          \t"os"
          \t"testing"
          )''',
              '''import (
          \t"errors"
          \t"os"
          \t"path/filepath"
          \t"testing"
          )''',
              "device test imports",
          )
          replace_once(device_tests, 'path := fakeBin + "/lsblk"', 'path := filepath.Join(fakeBin, "lsblk")', "first fake lsblk path")
          replace_once(
              device_tests,
              't.Setenv("PATH", fakeBin+string(os.PathListSeparator)+os.Getenv("PATH"))',
              'useDeviceUtility(t, "lsblk", path)',
              "first device PATH injection",
          )
          replace_once(
              device_tests,
              '''\tif err := os.WriteFile(fakeBin+"/lsblk", []byte(script), 0o755); err != nil {
          \t\tt.Fatal(err)
          \t}''',
              '''\tpath := filepath.Join(fakeBin, "lsblk")
          \tif err := os.WriteFile(path, []byte(script), 0o755); err != nil {
          \t\tt.Fatal(err)
          \t}''',
              "second fake lsblk path",
          )
          replace_once(
              device_tests,
              't.Setenv("PATH", fakeBin+string(os.PathListSeparator)+os.Getenv("PATH"))',
              'useDeviceUtility(t, "lsblk", path)',
              "second device PATH injection",
          )
          device_text = device_tests.read_text(encoding="utf-8")
          device_text += r'''

func useDeviceUtility(t *testing.T, expectedName, path string) {
	t.Helper()
	previous := resolveDeviceUtility
	resolveDeviceUtility = func(name string) (string, error) {
		if name != expectedName {
			return "", errors.New("unexpected utility request: " + name)
		}
		return path, nil
	}
	t.Cleanup(func() { resolveDeviceUtility = previous })
}
'''
          device_tests.write_text(device_text, encoding="utf-8")

          safety_source = Path("internal/safety/safety_linux.go")
          replace_once(safety_source, '\t"os/exec"\n', '', "remove ambient exec import")
          safety_text = safety_source.read_text(encoding="utf-8")
          if safety_text.count("device.Find(") != 3:
              raise SystemExit(f"safety device finder: expected 3 anchors, found {safety_text.count('device.Find(')}")
          safety_source.write_text(safety_text.replace("device.Find(", "findSafetyDevice("), encoding="utf-8")
          replace_once(
              safety_source,
              '''\t\tcmd := exec.CommandContext(ctx, "umount", "--", mountpoint)
          \t\tvar stderr bytes.Buffer
          \t\tcmd.Stderr = &stderr
          \t\terr := cmd.Run()
          \t\tcancel()''',
              '''\t\tcmd, commandErr := trustedSafetyCommandContext(ctx, "umount", "--", mountpoint)
          \t\tif commandErr != nil {
          \t\t\tcancel()
          \t\t\treturn fmt.Errorf("unmount %s: %w", mountpoint, commandErr)
          \t\t}
          \t\tvar stderr bytes.Buffer
          \t\tcmd.Stderr = &stderr
          \t\terr := cmd.Run()
          \t\tcancel()''',
              "trusted umount",
          )
          replace_once(
              safety_source,
              '''func runCommand(ctx context.Context, name string, args ...string) error {
          \tcmd := exec.CommandContext(ctx, name, args...)
          \tvar stderr bytes.Buffer''',
              '''func runCommand(ctx context.Context, name string, args ...string) error {
          \tcmd, err := trustedSafetyCommandContext(ctx, name, args...)
          \tif err != nil {
          \t\treturn err
          \t}
          \tvar stderr bytes.Buffer''',
              "trusted runCommand",
          )
          replace_once(
              safety_source,
              '''\tcmd := exec.CommandContext(ctx, name, args...)
          \tvar stdout, stderr bytes.Buffer''',
              '''\tcmd, err := trustedSafetyCommandContext(ctx, name, args...)
          \tif err != nil {
          \t\treturn "", err
          \t}
          \tvar stdout, stderr bytes.Buffer''',
              "trusted commandOutput",
          )

          safety_tests = Path("internal/safety/safety_linux_test.go")
          replace_once(
              safety_tests,
              '''import (
          \t"context"
          \t"os"''',
              '''import (
          \t"context"
          \t"errors"
          \t"os"''',
              "safety test imports",
          )
          replace_once(
              safety_tests,
              't.Setenv("PATH", fakeBin+string(os.PathListSeparator)+os.Getenv("PATH"))',
              '''useSafetyUtilities(t, map[string]string{
          \t\t"findmnt": filepath.Join(fakeBin, "findmnt"),
          \t\t"lsblk":   filepath.Join(fakeBin, "lsblk"),
          \t})''',
              "backing disk utility injection",
          )
          replace_once(
              safety_tests,
              't.Setenv("PATH", fakeBin+string(os.PathListSeparator)+os.Getenv("PATH"))',
              'useSafetyUtilities(t, map[string]string{"umount": filepath.Join(fakeBin, "umount")})',
              "umount utility injection",
          )
          replace_once(
              safety_tests,
              '''func TestEnsureNoMountedDescendantsFailsClosed(t *testing.T) {
          \tfakeBin := t.TempDir()
          \tjsonOutput := `{"blockdevices":[{"name":"sda","path":"/dev/sda","type":"disk","size":1000,"model":"","vendor":"","tran":"usb","rm":0,"ro":0,"hotplug":1,"mountpoints":[null],"pkname":null,"maj:min":"8:0","serial":"","wwn":"","children":[{"name":"sda1","path":"/dev/sda1","type":"part","size":900,"model":"","vendor":"","tran":"","rm":0,"ro":0,"hotplug":0,"mountpoints":["/media/usb"],"pkname":"sda","maj:min":"8:1","serial":"","wwn":""}]}]}`
          \twriteFake(t, filepath.Join(fakeBin, "lsblk"), "#!/bin/sh\\nprintf '%s\\n' '"+jsonOutput+"'\\n")
          \tt.Setenv("PATH", fakeBin+string(os.PathListSeparator)+os.Getenv("PATH"))
          \tif err := EnsureNoMountedDescendants("/dev/sda"); err == nil || !strings.Contains(err.Error(), "mounted again") {
          \t\tt.Fatalf("expected mounted-target refusal, got %v", err)
          \t}
          }''',
              '''func TestEnsureNoMountedDescendantsFailsClosed(t *testing.T) {
          \tuseSafetyDeviceFinder(t, func(path string) (device.BlockDevice, error) {
          \t\treturn device.BlockDevice{
          \t\t\tPath: path,
          \t\t\tType: "disk",
          \t\t\tSize: 1000,
          \t\t\tTransport: "usb",
          \t\t\tChildren: []device.BlockDevice{{
          \t\t\t\tPath: "/dev/sda1", Type: "part", Size: 900, Mountpoints: []string{"/media/usb"},
          \t\t\t}},
          \t\t}, nil
          \t})
          \tif err := EnsureNoMountedDescendants("/dev/sda"); err == nil || !strings.Contains(err.Error(), "mounted again") {
          \t\tt.Fatalf("expected mounted-target refusal, got %v", err)
          \t}
          }''',
              "mounted descendant finder injection",
          )
          insert = '''func writeFake(t *testing.T, path, content string) {'''
          helpers = r'''func useSafetyUtilities(t *testing.T, paths map[string]string) {
	t.Helper()
	previous := resolveSafetyUtility
	resolveSafetyUtility = func(name string) (string, error) {
		path, ok := paths[name]
		if !ok {
			return "", errors.New("unexpected utility request: " + name)
		}
		return path, nil
	}
	t.Cleanup(func() { resolveSafetyUtility = previous })
}

func useSafetyDeviceFinder(t *testing.T, finder func(string) (device.BlockDevice, error)) {
	t.Helper()
	previous := findSafetyDevice
	findSafetyDevice = finder
	t.Cleanup(func() { findSafetyDevice = previous })
}

func TestUtilityResolutionFailureIsFailClosed(t *testing.T) {
	previous := resolveSafetyUtility
	resolveSafetyUtility = func(string) (string, error) { return "", errors.New("unsafe utility") }
	t.Cleanup(func() { resolveSafetyUtility = previous })
	if err := FlushBuffers(context.Background(), "/dev/sda"); err == nil || !strings.Contains(err.Error(), "unsafe utility") {
		t.Fatalf("utility resolution failure was not propagated: %v", err)
	}
}

'''
          replace_once(safety_tests, insert, helpers + insert, "safety test helpers")
          PY

          gofmt -w \
            internal/trustedexec/*.go \
            internal/device/device.go \
            internal/device/device_test.go \
            internal/device/utility_linux.go \
            internal/safety/safety_linux.go \
            internal/safety/safety_linux_test.go \
            internal/safety/utility_linux.go

      - name: Run focused race, vet and cross-build validation
        shell: bash
        run: |
          set -euo pipefail
          go test -race ./internal/trustedexec ./internal/device ./internal/safety ./cmd/rufus-linux
          go vet ./internal/trustedexec ./internal/device ./internal/safety ./cmd/rufus-linux
          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -trimpath -o /tmp/rufusarm64-cli ./cmd/rufus-linux
          test -s /tmp/rufusarm64-cli

      - name: Restore and strengthen permanent audit workflow
        shell: bash
        run: |
          set -euo pipefail
          git checkout origin/stage3/0.14.0 -- .github/workflows/ffu-software-audit.yml
          python3 - <<'PY'
          from pathlib import Path
          path = Path('.github/workflows/ffu-software-audit.yml')
          text = path.read_text(encoding='utf-8')
          text = text.replace("      - 'internal/safety/**'\n", "      - 'internal/safety/**'\n      - 'internal/trustedexec/**'\n")
          text = text.replace(
              'go test -race ./internal/ffu ./internal/device ./internal/safety ./cmd/rufus-linux',
              'go test -race ./internal/ffu ./internal/device ./internal/safety ./internal/trustedexec ./cmd/rufus-linux',
          )
          text = text.replace(
              'go vet ./internal/ffu ./internal/device ./internal/safety ./cmd/rufus-linux',
              'go vet ./internal/ffu ./internal/device ./internal/safety ./internal/trustedexec ./cmd/rufus-linux',
          )
          old = """          executor = Path('internal/ffu/restore_executor_linux.go').read_text(encoding='utf-8')

          required = {"""
          new = """          executor = Path('internal/ffu/restore_executor_linux.go').read_text(encoding='utf-8')
          device_source = Path('internal/device/device.go').read_text(encoding='utf-8')
          safety_source = Path('internal/safety/safety_linux.go').read_text(encoding='utf-8')
          trusted = Path('internal/trustedexec/trustedexec_linux.go').read_text(encoding='utf-8')

          required = {"""
          if text.count(old) != 1:
              raise SystemExit('permanent audit source anchor changed')
          text = text.replace(old, new)
          old = """              'unknown target fail-closed warning': (dialog, 'target state is unknown'),
          }"""
          new = """              'unknown target fail-closed warning': (dialog, 'target state is unknown'),
              'trusted utility resolver': (trusted, 'Ambient PATH is never consulted.'),
              'device trusted lsblk': (device_source, 'resolveDeviceUtility("lsblk")'),
              'safety trusted commands': (safety_source, 'trustedSafetyCommandContext'),
          }"""
          if text.count(old) != 1:
              raise SystemExit('permanent audit requirement anchor changed')
          text = text.replace(old, new)
          old = """          joined = '\\n'.join((cli, binding, dialog, restore))
          for label, needle in forbidden.items():"""
          new = """          joined = '\\n'.join((cli, binding, dialog, restore, device_source, safety_source, trusted))
          for label, needle in forbidden.items():"""
          if text.count(old) != 1:
              raise SystemExit('permanent audit joined-source anchor changed')
          text = text.replace(old, new)
          text = text.replace(
              "              'confirmation bypass': '--yes',\n",
              "              'confirmation bypass': '--yes',\n              'ambient utility lookup': 'exec.LookPath',\n",
          )
          path.write_text(text, encoding='utf-8')
          PY

      - name: Commit validated final source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .github/workflows/ffu-software-audit.yml internal/device internal/safety internal/trustedexec
          git commit -m '[trusted-patched] safety: resolve privileged utilities explicitly'
          git push origin HEAD:feature/ffu-trusted-utilities
