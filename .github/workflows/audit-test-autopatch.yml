name: Audit test autopatch

on:
  push:
    branches:
      - audit/pre-0.11-hardening

permissions:
  contents: write

concurrency:
  group: audit-test-autopatch-${{ github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: audit/pre-0.11-hardening
          fetch-depth: 0
      - run: python3 scripts/pre011_test_patch.py
      - run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add internal/windowsmedia/windowsmedia_test.go gui/test_logic.py
          git commit -m "test: enforce audited privilege and persistence UI contracts"
          git push origin HEAD:audit/pre-0.11-hardening
